import { returnHello } from './returnHello'
import { socketLogin } from './socketLogin'
import { socketToken } from './socketToken'

export const Actions = {
    returnHello,
    socketLogin,
    socketToken
}
